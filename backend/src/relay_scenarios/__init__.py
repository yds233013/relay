"""Scenario generators for fictional migrations.

EVALUATION/DEMO-ONLY PACKAGE. Generators know which defects they plant. Runtime code in the
``relay`` package must never import this package (enforced by import-linter and tests).
"""
